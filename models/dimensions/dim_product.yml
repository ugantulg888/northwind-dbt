version: 2

models:
  - name: dim_product
    description: Product Dimension including supplier and category attributes.
    columns:
      - name: productkey
        description: Surrogate key for the product
        tests:
          - not_null
          - unique

      - name: productid
        description: Business key from the source system
        tests:
          - not_null
          - unique