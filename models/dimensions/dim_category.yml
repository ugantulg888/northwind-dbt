version: 2

models:
  - name: dim_category
    description: Category Dimension. One row per product category.
    columns:
      - name: categorykey
        description: Surrogate key for the category
        tests:
          - not_null
          - unique

      - name: categoryid
        description: Business key from the source system
        tests:
          - not_null
          - unique